<!DOCTYPE html>
<html lang="en">

<head>
  {{include "." "web/page/admin" "head"}} 
</head>

<body>

  <div role="dialog">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <a class="close" href="{{req_path R.PATH_APPEND "../" "list"}}"><span aria-hidden="true">&times;</span></a>
          <h4 class="modal-title">Create Web Page</h4>
        </div>
        <div class="modal-body">

          <ol class="breadcrumb">
            <li class="active">HOME</a></li>
            {{#include "." "parents"}}
              <li class="active">{{name}}</li>
            {{/include}}
            <li>{{R.NAME}}</li>
          </ol>

          <h6>Create ObjectResource as:</h6>

          <div class="form-horizontal">
            <div class="form-horizontal">
              <form method="post" action="{{R.PATH_APPEND}}{:name}">
                <div class="form-group">
                  <label class="col-sm-4">render type</label>
                  <div class="col-sm-6">
                    <select class="form-control" name=":copyfrom">
                    {{#include "." "resourceblueprints"}}
                      <option value="{{path}}">{{name}}</option>
                    {{/include}}
                    </select>
                  </div>
                </div>

                <div class="form-group">
                  <div class="col-sm-8">
                    <input class="form-control" name=":name|newUUID" value="" autocomplete="off" placeholder="name">
                    <input type="hidden" name=":forward" value="{{ref_path R.PATH "list"}}">
                    <input type="hidden" name="title" value="{:name}">
                  </div>
                  <div class="col-sm-2">
                    <button class="form-control btn btn-success" type="submit">
                      <span class="glyphicon glyphicon-plus"></span>
                    </button>
                  </div>
                </div>
              </form>
            </div>
          </div>

        </div>
        <div class="modal-footer">
          <a class="btn btn-default" href="{{ref_path R.PATH "list"}}">Cancel</a>
        </div>
      </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
  </div><!-- /.modal -->

</body>

</html>
